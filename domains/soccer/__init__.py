"""domains.soccer — market-only club soccer adapter (O/U 2.5 goals, football-data.co.uk).

Pure-new-file tranche: data ingest (football-data.co.uk match/odds CSVs) + leak-free
walk-forward goals-rate model.  Imports ZERO src.* / domains.nba (falsifier F5).
The CV_DOMAIN_SOCCER flag + full adapter wiring are deferred to the baseline window.
"""

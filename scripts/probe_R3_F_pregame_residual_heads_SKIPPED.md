# R3-F Pregame Residual Heads — SKIPPED

**Date:** 2026-05-25
**Phase:** R3-F (pregame residual heads)
**Status:** SKIPPED — blocked on OOF cache

## Why Skipped

R3-F requires out-of-fold (OOF) pergame predictions as input to train residual heads.
These are generated by running XGB + LGB + MLP across 5-fold cross-validation on all
7 stats (PTS, REB, AST, FG3M, STL, BLK, TOV), which takes 3–5 hours.

Neither `data/predictions/pregame*.csv` nor `data/cache/pergame_oof*.parquet` (nor any
variant) was found. Both directories exist but contain no OOF prediction files.

## Path Forward

1. **Queue `scripts/cache_pergame_oof.py`** as a dedicated cycle to:
   - Run 5-fold CV on the full prop_pergame feature set
   - Save per-fold OOF predictions to `data/cache/pregame_oof.parquet`
     (columns: `player_id`, `game_id`, `stat`, `y_true`, `y_hat_oof`, `fold`)
2. **Once `data/cache/pregame_oof.parquet` exists**, R3-F can:
   - Compute residuals: `residual = y_true - y_hat_oof`
   - Train lightweight residual head models (XGB/LGB/MLP) on game-day context features
   - Evaluate via walk-forward MAE delta vs base predictions
   - Gate: ship only if WF MAE strictly improves across >= 5/7 stats

## Estimated Cost

- `cache_pergame_oof.py`: ~3–5 hr on CPU, ~45 min on GPU (RunPod)
- R3-F residual head training (once cache exists): ~10–20 min

# Signal Lab -- gated wiring proposals (validated signals only; nothing auto-applied)

- **rest_x_age** (player-game, rmse -0.240%, split-half -0.17/-0.09, ortho 0.143): modulate **per-player pts target on high-rest games for older players** via `pts *= 1 + SLOPE*rest*(age-27) (centered at neutral)` behind `CV_SIG_RESTAGE` (default OFF). Requires a board-green A/B before any flip; NOT auto-applied.
- **shot_clock_leverage** (possession, rmse -2.860%, split-half -0.14/-0.13, ortho 0.306): modulate **xFG make probability (shot-quality by shot-clock state)** via `xfg *= 1 + SLOPE*(poss_clock_state - neutral)` behind `CV_SIG_SHOTCLOCK` (default OFF). Requires a board-green A/B before any flip; NOT auto-applied. CROSS-SEASON CONFIRMED (build_legacy_possessions.py, 560k possessions): replicates league-wide 2022-23 (-2.12%) + 2023-24 (-2.08%) + 2025-26 (-2.86%), split-half stable -0.17 each -> NOT a single-window peak; the strongest signal in the registry. NOTE: poss_dur is co-determined with the shot, so wire it as the generative xFG-vs-clock-state CURVE, not a feature.
- **opp_position_defense_reb** (player-game, rmse -0.440%, split-half -0.19/-0.26, ortho 0.386): modulate **TBD (map the node)** via `centered multiplier` behind `CV_SIG_OPP_POSITION_DEFENSE_REB` (default OFF). Requires a board-green A/B before any flip; NOT auto-applied. CAVEAT: SIGN IS BACKWARDS (corr −0.19/−0.26: more-allowed -> FEWER reb) and n=190 -- passes the gates but the effect is opposite the causal story = a likely pace/selection confound or small-n artifact. DO NOT WIRE; resolve the sign (control for pace) first. Illustrates that the lab's gates are necessary, NOT sufficient -- sign+n+engine-redundancy need a judgment layer.
- **oreb_matchup** (team-game, rmse -0.540%, split-half +0.18/+0.19, ortho 0.251): modulate **second-chance points from own OREB% vs opp allowed-OREB** via `oreb_p *= 1 + SLOPE*(oreb_matchup - neutral)` behind `CV_SIG_OREB` (default OFF). Requires a board-green A/B before any flip; NOT auto-applied. CAVEAT: orthogonal only to the coarse net-diff baseline; the engine ALREADY models OREB (`oreb_per_miss`) so this LIKELY DOUBLE-COUNTS (cf the M3 team-total finding) -- the A/B MUST screen orthogonality vs the engine's OREB output before any flip.
